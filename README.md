# Simple Interest Calculator

This project calculates simple interest using the formula:

Simple Interest = (Principal * Rate * Time) / 100

## Usage
Run the Bash script and enter:
- Principal amount
- Rate of interest
- Time period
